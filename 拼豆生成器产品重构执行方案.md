# 拼豆生成器产品重构执行方案

## 1. 背景与目标

当前项目已经具备图片量化、手工编辑、材料统计、可拼性检测、PNG/PDF/CSV 导出和本地工程保存等能力，但用户首次进入后需要理解多个页面和侧栏，主任务不够突出，容易产生“工具很多但不知道先做什么”的感受。

本次重构的目标不是继续堆功能，而是把体验收束为一条清晰路径：

> 上传图片 → 选择制作方案 → 得到可购买、可拼装、可打印的拼豆图纸 → 需要时进入精细编辑

成功标准：一个第一次使用的用户无需阅读教程，也能在 1 分钟内生成并导出一份可执行的拼豆图纸。

## 2. 借鉴原则与边界

参考产品：[BitBead](https://www.bitbead.app/zh/generator)。

可借鉴：

- 围绕“生成可制作图纸”的产品流程。
- 将品牌、尺寸、色数、清理和预览集中在同一工作台的交互结构。
- 生成阶段提供自动去背景、限制色数、减少零散颜色、清理孤立区域等能力。
- 在生成结果旁实时展示材料与制作数据。

不可直接复制：

- 对方源代码、页面结构、视觉设计、图标、插图、文案和品牌素材。
- 对方整理的色卡数据库、图纸库内容和用户数据。

本项目应实现相同类别的通用功能，但采用自主的视觉、文案、代码和经过独立核验的色卡数据。

## 3. 目标产品结构

### 3.1 首页：只回答“怎么开始”

首页首屏只保留一个明确的主任务：上传图片开始生成。

- 主按钮：`上传图片，生成拼豆图纸`。
- 辅助入口：从空白画布创作、打开本地工程、查看尺寸模板。
- 清楚展示用户最终可得到的内容：图纸、材料清单、实物尺寸、拼板数量和打印文件。
- 原有尺寸模板保留，但下移为“常用制作尺寸”，不应抢占首次上传图片的主流程。

### 3.2 生成工作台：先生成，再精修

建立独立的生成工作台，替代当前“先进入完整编辑器，再弹出导入窗口”的结构。

| 区域 | 内容 | 设计目的 |
| --- | --- | --- |
| 左侧：生成参数 | 上传/更换图片、构图、尺寸、色数、预设、自动整理 | 集中完成所有生成决策 |
| 中间：大预览 | 成品效果与制作图纸切换、缩放、网格/色号开关 | 让用户随时看见结果 |
| 右侧：制作结果 | 总颗数、真实尺寸、拼板数量、用色数、材料清单、可拼性提示 | 把“好不好看”转成“能不能做” |
| 底部主操作 | 重新生成、进入精细编辑、导出图纸 | 明确下一步，避免隐藏能力 |

生成工作台默认展示简化参数；高级选项收进“优化图纸”折叠区，避免首次用户被大量选项吓退。

### 3.3 精细编辑：只服务需要微调的用户

保留现有编辑器作为第二阶段工具，而不是默认入口。

- 画笔、橡皮、吸管、填充、撤销/重做保持可用。
- 继续保留颗粒、网格、像素三种视图。
- 保留材料高亮、孤岛检测和一键修复。
- 编辑页顶部增加“返回生成参数”入口，允许重新量化，而不丢失当前结果前需明确提示。

## 4. 功能清单与优先级

### P0：必须完成，解决“生硬、杂乱、不会用”

1. 首页主入口调整为上传图片生成。
2. 新增生成工作台，展示图片、关键参数、实时预览和制作数据。
3. 将当前导入弹窗中的能力迁移/复用到生成工作台：裁切、整图放入、自动比例、裁切铺满、尺寸、色数、预设和实时预览。
4. 将材料统计、实物尺寸、拼板数量移到生成结果右侧实时展示。
5. 保留“进入精细编辑”作为次级路径。
6. 保留现有 PNG、PDF、CSV、`.bead` 导出能力，并从工作台提供明确入口。

### P1：必须完成，提升图纸的实际可制作性

1. 增加自动去背景。
   - 透明像素保留为空格。
   - 支持根据边缘连通区域去除相近背景色。
   - 不应误删图片内部同色区域。
2. 增加“少量颜色合并”。
   - 按用量由少到多选择需要合并的色号。
   - 合并目标应是当前已使用颜色中视觉距离最近的颜色。
   - 预览和材料统计必须同步更新。
3. 增加“小区域清理”。
   - 识别过小的连通区域，将其合并为相邻主色。
   - 不使用仅按单格判断的规则，避免误伤有效细节。
4. 将现有“孤岛检测”前移到生成阶段。
   - 生成时给出风险数量和可选修复。
   - 精细编辑阶段仍保留高亮与一键修复。
5. 预设统一为：`细节优先`、`易拼优先`、`复古像素`。
   - 每个预设应同时定义推荐色数、抖动、自动整理强度，不能只改变文案。

### P2：必须完成，建立真实拼豆色卡体系

1. 将当前单一标准色板升级为“色卡品牌”数据模型。
2. 初期支持的品牌由产品负责人确认；建议按目标用户常用品牌分批上线。
3. 每个色卡包含：品牌 ID、色号、中文名/原始名、HEX、RGB、豆粒规格、状态（在售/停产/待核验）。
4. 量化、画布色号、材料清单、CSV、PNG、PDF 必须引用同一个色卡来源。
5. 用户切换色卡后，应重新量化并明确告知“材料色号会变化”。
6. 色卡数据必须独立收集和核验，记录来源与核验日期；不得抓取或复制第三方工具整理的数据表。

### P3：增强项，待 P0-P2 稳定后再评估

- 项目历史与最近作品。
- 可分享链接、公开图纸库。
- 社区作品收藏和搜索。
- 账号、云同步和会员体系。

说明：P3 会引入服务端、图片存储、版权投诉、内容审核和隐私合规成本，不属于当前“先做一个人人愿意用的工具”的必要范围。

## 5. 技术改造范围

| 模块 | 当前文件/逻辑 | 改造方向 | 影响 |
| --- | --- | --- | --- |
| 首页 | `src/app/page.tsx` | 调整为上传优先的生成入口 | 首页交互与文案 |
| 页面路由 | `src/app/editor/page.tsx` | 增加或拆分生成工作台路由 | 入口跳转 |
| 编辑总布局 | `src/components/editor/EditorWorkspace.tsx` | 支持“生成模式 / 精细编辑模式”或拆分独立工作台 | 编辑流程 |
| 导入生成 | `src/components/editor/CropModal.tsx` | 抽离为可复用生成参数、预览和上传模块 | 生成体验 |
| 画布 | `src/components/editor/EditorCanvas.tsx` | 支持工作台预览与制作图纸预览 | 画布显示 |
| 侧栏 | `src/components/editor/EditorSidebar.tsx` | 拆分材料结果、规格、可拼性结果组件 | 数据呈现 |
| 状态管理 | `src/store/useEditorStore.ts` | 记录色卡、生成选项、生成结果与编辑状态 | 全局调用方 |
| 量化 | `src/lib/quantize/quantizeEngine.ts` | 去背景、色号合并、小区域清理、预设参数 | 图像生成结果 |
| 类型 | `src/lib/types.ts` | 增加色卡、生成优化参数和结果统计类型 | 所有引用类型的模块 |
| 色板 | `src/lib/palette/standardPalette.ts` | 升级为可扩展的多色卡数据组织 | 量化、画布、导出 |
| 导出 | `src/lib/export/*` | 统一引用真实品牌色号和新的图纸元数据 | PNG/PDF/CSV 输出 |

## 6. 建议实施顺序

### 阶段 A：先验证核心体验

1. 设计并实现生成工作台骨架。
2. 复用已有量化、预览、材料统计、导出和精细编辑能力。
3. 将首页入口导向工作台。
4. 用标准色板先完成全链路验证。

验收：用户上传图片后，能在同一页面调整尺寸、色数和预设，并立即看到图纸及材料数据；可进入精细编辑或导出。

### 阶段 B：提高实际制作成功率

1. 接入自动去背景、少量色号合并、小区域清理。
2. 统一预设对算法参数的影响。
3. 将可拼性检测加入生成结果。

验收：同一张复杂照片使用“易拼优先”时，颜色数和孤立区域显著少于“细节优先”，且用户可以预览差异。

### 阶段 C：接入首批品牌色卡

1. 完成色卡数据模型与选择器。
2. 接入一套经核验的首发品牌色卡。
3. 确保量化、画布、材料清单与导出结果一致。
4. 再按实际用户需求逐个增加品牌。

验收：用户选择品牌后，图纸中每个颜色、清单和导出文件都展示相同、可采购的色号。

## 7. 测试与验收清单

- 上传 PNG、JPG、WebP 后均可正常生成。
- 横图、竖图、正方形图在三种构图模式下不变形，符合预期裁切。
- 调整宽高、色数、预设、抖动和自动整理后，预览、总数和材料清单同步更新。
- 透明背景与去背景结果能正确保留空格。
- 不同色卡之间的切换会重新量化，色号、材料统计、画布和导出保持一致。
- “进入精细编辑”后，生成结果完整保留；编辑后的统计与导出正确。
- 大尺寸图纸导出 PDF 时，分页、色号、1:1 比例和材料清单正确。
- 现有 `.bead` 导入导出兼容；旧工程打开后能正常编辑。
- 对量化、连通区域清理、材料统计和导出分页补充单元测试；对上传至导出主链路补充端到端测试。

## 8. 风险与处理原则

| 风险 | 处理原则 |
| --- | --- |
| 色卡不准确导致采购失败 | 仅使用独立核验的数据；保留数据来源、核验日期和版本；先少量品牌上线 |
| 自动清理损失细节 | 默认提供预览和可撤销的选项；不能静默修改用户已手动编辑的画布 |
| 参数过多重新造成复杂 | 默认只显示尺寸、色数、预设；高级整理参数收进折叠区 |
| 大图量化卡顿 | 量化采用防抖、可取消任务；必要时迁移到 Web Worker |
| 重构破坏既有编辑与导出 | 先抽离可复用组件与状态；每阶段覆盖现有工程、编辑、导出测试 |
| 图纸库/社区提前膨胀范围 | P0-P2 完成并验证留存前，不引入服务端社区功能 |

## 9. 当前工作区说明

本方案只新增文档，不包含任何产品代码改动。

编写方案时，工作区已有以下未提交改动，均应视为现有工作并保持不动：

- `src/components/editor/CropModal.tsx`
- `src/components/editor/EditorCanvas.tsx`
- `src/lib/quantize/quantizeEngine.ts`
- `src/lib/types.ts`

